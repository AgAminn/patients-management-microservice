#!/usr/bin/env groovy

pipeline {
    agent any

    environment {
        def config = readYaml file: 'Jenkins.yaml'
            GIT_BRANCH = config.parameters.GIT_BRANCH
            BUILD_TYPE = config.parameters.BUILD_TYPE
    }

    stages {
        
        stage ("Init" ) {
            steps {
                echo 'welcome to Code Review Pipeline'
                echo 'Init env values'
                

            }
        }

        stage ("Test" ) {
            steps {
                echo 'check code quqlity'
                
            }
        }

        stage ("Build" ) {
            steps {
                echo 'Build docker image'
                // docker build -t jkz-app:0.1 .
                
            }
        }

        stage ("Publish" ) {
            steps {
                echo 'Publish docker image'
                
            }
        }

        stage ("Deploy" ) {
            steps {
                echo 'Deploy pod -- helm install'
            }
        }

        stage ("Clean Up" ) {
            steps {
                echo 'remove dangling docker images'
            }
        }

    }
}